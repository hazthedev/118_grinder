---
export interface Props {
  class?: string;
  as?: 'div' | 'section' | 'article' | 'header' | 'footer';
}

const { class: className = '', as: Tag = 'div' } = Astro.props;
---

<Tag class={`mx-auto max-w-7xl px-5 sm:px-8 lg:px-12 ${className}`}>
  <slot />
</Tag>
